---
import { Image } from "astro:assets";
import BaseLayout from "../../layouts/BaseLayout.astro";
import HeroBreadcrumb from "../../components/HeroBreadcrumb.astro";
import { services, type Service } from "../../data/services";
import { site } from "../../data/site";

import imgThuongMai from "../../../public/images/thuong-mai-quoc-te-hero.jpg";
import imgDanSu from "../../../public/images/dan-su-hop-dong-hero.jpg";
import imgBaoChua from "../../../public/images/bao-chua-hinh-su-hero.jpg";
import imgTuVan from "../../../public/images/tu-van-phap-ly-hinh-su-hero.png";
import imgDatDai from "../../../public/images/dat-dai-bat-dong-san-hero.jpg";
import imgNghienCuu from "../../../public/images/nghien-cuu-tu-van-chinh-sach-phap-luat-hero.jpg";

const heroImages: Record<string, ImageMetadata> = {
  "thuong-mai-quoc-te": imgThuongMai,
  "dan-su-hop-dong": imgDanSu,
  "bao-chua-hinh-su": imgBaoChua,
  "tu-van-phap-ly-hinh-su": imgTuVan,
  "dat-dai-bat-dong-san": imgDatDai,
  "nghien-cuu-tu-van-chinh-sach-phap-luat": imgNghienCuu,
};

export function getStaticPaths() {
  return services.map((service) => ({
    params: { slug: service.slug },
    props: { service },
  }));
}

interface Props {
  service: Service;
}

const { service } = Astro.props;
const title = `${service.title} | ${site.name}`;
const description = service.summary;
const canonicalPath = `/dich-vu/${service.slug}/`;
const serviceUrl = new URL(canonicalPath, site.canonicalBaseUrl).toString();
const structuredData = [
  {
    "@context": "https://schema.org",
    "@type": "Service",
    name: service.title,
    description: service.summary,
    url: serviceUrl,
    provider: {
      "@type": "LegalService",
      name: site.name,
      url: site.canonicalBaseUrl,
      ...(site.publicPhone ? { telephone: site.publicPhone } : {}),
    },
    inLanguage: "vi-VN",
  },
  {
    "@context": "https://schema.org",
    "@type": "BreadcrumbList",
    itemListElement: [
      {
        "@type": "ListItem",
        position: 1,
        name: "Trang chủ",
        item: site.canonicalBaseUrl,
      },
      {
        "@type": "ListItem",
        position: 2,
        name: "Dịch vụ",
        item: new URL("/dich-vu/", site.canonicalBaseUrl).toString(),
      },
      {
        "@type": "ListItem",
        position: 3,
        name: service.title,
        item: serviceUrl,
      },
    ],
  },
];
---

<BaseLayout title={title} description={description} canonicalPath={canonicalPath} structuredData={structuredData}>

  <section class="relative overflow-hidden py-24 md:py-32 lg:py-40">

    <div class="absolute inset-0 z-0 bg-ink-black">
      <Image
        src={heroImages[service.slug]}
        alt={service.title}
        class="h-full w-full object-cover object-center"
        loading="eager"
        fetchpriority="high"
      />

      <div class="absolute inset-0 bg-black/50"></div>
    </div>


    <div class="section-shell relative z-10">
      <HeroBreadcrumb items={[{ label: "Trang chủ", href: "/" }, { label: "Dịch vụ", href: "/dich-vu/" }, { label: service.title }]} />

      <h1 class="mt-8 max-w-3xl font-sans text-[2.25rem] font-semibold leading-[1.14] tracking-[-0.01em] text-white md:text-[2.75rem] lg:text-[3.125rem]">
        {service.title}
      </h1>

      <p class="mt-6 max-w-xl font-sans text-[1rem] leading-[1.8] text-white/80 md:text-[1.0625rem]">
        {service.summary}
      </p>
    </div>
  </section>



  <section class="bg-panel-cream">
    <div class="section-shell py-16 md:py-24">
      <div class="grid gap-10 md:grid-cols-[200px_1fr] lg:grid-cols-[240px_1fr]">
        <div class="pt-1">
          <p class="font-sans text-[1.0625rem] font-medium text-text-dark">Tổng quan</p>
          <div class="mt-3 h-px w-8 bg-text-dark"></div>
        </div>
        <div class="font-sans text-[1rem] leading-[1.85] text-text-dark">
          <p>{service.description}</p>
        </div>
      </div>
    </div>
  </section>


  <section class="bg-panel-taupe border-t border-panel-taupe-border">
    <div class="section-shell py-16 md:py-24">
      <div class="grid gap-10 md:grid-cols-[200px_1fr] lg:grid-cols-[240px_1fr]">
        <div class="pt-1">
          <p class="font-sans text-[1.0625rem] font-medium text-text-dark">Chúng tôi hỗ trợ</p>
          <div class="mt-3 h-px w-8 bg-text-dark"></div>
        </div>
        <div class="font-sans text-[1rem] leading-[1.85] text-text-dark space-y-5">
          {service.situations.map((item) => (
            <p>{item}</p>
          ))}
        </div>
      </div>
    </div>
  </section>
</BaseLayout>
